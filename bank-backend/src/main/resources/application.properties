# ==========================================
# ??????? (Database Configuration)
# ==========================================
# ?????${??????:???}
# ????????????? (?? Railway ??? MYSQLHOST)???????????????? (localhost)?

# ????? URL
# ???jdbc:mysql://????:???/?????
# ? Railway ?????? MYSQLHOST, MYSQLPORT ??????????? localhost:3306/banking_app
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:banking_app}

# ???????? (??? root)
spring.datasource.username=${MYSQLUSER:root}

# ????? (??? root)
spring.datasource.password=${MYSQLPASSWORD:root}

# ?? JDBC Driver?MySQL 8.x ???? com.mysql.cj.jdbc.Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================
# JPA / Hibernate ?? (ORM Configuration)
# ==========================================
# DDL ????
# update: ??????? Entity ???????????????????? (??????)?
# validate: ??????????? Entity ??????????
# none: ?????? (??????)?
spring.jpa.hibernate.ddl-auto=update

# ? Console ?????? SQL ?? (?????????? false)
spring.jpa.show-sql=true

# ???? SQL ???????????????
spring.jpa.properties.hibernate.format_sql=true

# ???????
# ?? Hibernate ?? MySQL 8 ???????? SQL (??? MySQL 5.7 ??? MySQL5Dialect)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==========================================
# ????????? (App & Logging)
# ==========================================
# ?? Spring Boot ?????? ASCII Banner ??
spring.main.banner-mode=off

# ????????
# warn: ??? ??(Warn) ? ??(Error) ???????? Console ??
logging.level.root=warn

# ???????? (?? Railway / Heroku ?????????)
# ???????? Load Balancer ?????????? Spring Boot ????? HTTPS ?? (X-Forwarded-Proto)
# ??????? URL ?? https ?? http
server.forward-headers-strategy=framework

# ==========================================
# JWT ???? (JSON Web Token)
# ==========================================
# JWT ???? (Secret Key)
# ??????? Token ???????????????????????????????????????????
# ?????? JWT_SECRET????????????????????????
spring.app.jwtSecret=${JWT_SECRET:ThisIsADefaultSecretForLocalDevelopmentOnlyPleaseDoNotUseInProd}

# JWT Token ???? (?????)
# 172800000 ms = 172,800 ? = 48 ??
spring.app.jwtExpirationMs=172800000

spring.jackson.serialization.write-dates-as-timestamps=false
