server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;

        # 最重要的一行：先找實體檔案，找不到就全部導向 /index.html 給 React Router 處理
        try_files $uri $uri/ /index.html;
    }
}